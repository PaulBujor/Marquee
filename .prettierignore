# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/
/drizzle/
worker-configuration.d.ts

# Local tooling (git-ignored; keeps local `pnpm lint` in step with CI)
.claude/

# Throwaway PWA-asset tool output (source of truth is dev-assets/logo.png)
dev-assets/
